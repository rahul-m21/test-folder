[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
cryptography = ">=3.3.2"  # Non-PQC (example version; modify as per your database)
pycryptodome = "3.14.1"   # Non-PQC (example version)
pyca = "1.1.0"            # PQC (example library; modify as per your database)
openssl = "1.1.1"         # Non-PQC (example library)

[dev-packages]
pytest = "*"

[requires]
python_version = "3.8"
